# Dawud Charity Hub - Environment Variables Configuration
# Copy this file to .env and update with your actual values

# Security - Generate a secure random key for production
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-to-33122110

# CORS - Allowed origins for frontend (comma-separated)
# In production, replace with your actual domain(s)
ALLOWED_ORIGINS=https://kedi.furi-cadaster.com,https://www.kedi.furi-cadaster.com





# Rate Limiting (Optional - customize if needed)
# MAX_DONATION_SUBMISSIONS_PER_MINUTE=10
# MAX_LOGIN_ATTEMPTS_PER_MINUTE=5

# File Upload Settings (Screenshot/Image uploads)
MAX_FILE_SIZE_MB=5
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Session Settings
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email Settings (for future notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# ADMIN_EMAIL=admin@dawudcharity.org

